import{_ as t,c as o,a3 as a,o as n}from"./chunks/framework.B5QXucYL.js";const i="/dotpi/assets/dotpi-manager.DNj639ad.png",s="/dotpi/assets/dotpi-manager-global.DhWlB9M3.png",r="/dotpi/assets/dotpi-manager-bar.B_c7yV8t.png",l="/dotpi/assets/dotpi-manager-scripting.YrnUxdZY.png",c="/dotpi/assets/dotpi-manager-clients.D1apkSQG.png",p="/dotpi/assets/dotpi-manager-logs.e33xmLU4.png",k=JSON.parse('{"title":"Using the dotpi-manager","description":"","frontmatter":{},"headers":[],"relativePath":"using-the-dotpi-manager.md","filePath":"using-the-dotpi-manager.md"}'),d={name:"using-the-dotpi-manager.md"};function h(m,e,g,u,f,y){return n(),o("div",null,e[0]||(e[0]=[a('<h1 id="using-the-dotpi-manager" tabindex="-1">Using the <code>dotpi-manager</code> <a class="header-anchor" href="#using-the-dotpi-manager" aria-label="Permalink to &quot;Using the `dotpi-manager`&quot;">​</a></h1><p>The <code>dotpi-manager</code> is a <a href="http://soundworks.dev" target="_blank" rel="noreferrer"><em>soundworks</em></a> application aimed at managing and controlling fleets of RPi. It is composed of:</p><ul><li>a Node.js server that should run on your computer</li><li>a Browser client to monitor and control your fleet of devices — which is called <em>controller</em></li><li>a Node.js client that runs on the RPi and that is automatically installed by the <em>dotpi-install</em> tool.</li></ul><h2 id="installing-the-dotpi-manager" tabindex="-1">Installing the <code>dotpi-manager</code> <a class="header-anchor" href="#installing-the-dotpi-manager" aria-label="Permalink to &quot;Installing the `dotpi-manager`&quot;">​</a></h2><p>First, you need to install the <code>dotpi-manager</code>.</p><p>Choose a directory where you will install it, for e.g. <code>Documents</code>.</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki monokai vp-code" tabindex="0"><code><span class="line"><span style="color:#66D9EF;">cd</span><span style="color:#E6DB74;"> ~/Documents</span></span></code></pre></div><p>And clone the git project:</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki monokai vp-code" tabindex="0"><code><span class="line"><span style="color:#A6E22E;">git</span><span style="color:#E6DB74;"> clone</span><span style="color:#E6DB74;"> https://github.com/ircam-ismm/dotpi-manager.git</span></span></code></pre></div><p>Go to the project directory and install the dependencies with the <code>npm install</code> command:</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki monokai vp-code" tabindex="0"><code><span class="line"><span style="color:#66D9EF;">cd</span><span style="color:#E6DB74;"> ~/Documents/dotpi-manager/</span></span>\n<span class="line"><span style="color:#A6E22E;">npm</span><span style="color:#E6DB74;"> install</span></span></code></pre></div><h2 id="running-the-dotpi-manager" tabindex="-1">Running the <code>dotpi-manager</code> <a class="header-anchor" href="#running-the-dotpi-manager" aria-label="Permalink to &quot;Running the `dotpi-manager`&quot;">​</a></h2><p>To run the <em>dotpi-manager</em> application, go to its directory and execute the <code>npm run dev</code> command to launch the server:</p><div class="language-sh"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki monokai vp-code" tabindex="0"><code><span class="line"><span style="color:#66D9EF;">cd</span><span style="color:#E6DB74;"> ~/Documents/dotpi-manager/</span></span>\n<span class="line"><span style="color:#A6E22E;">npm</span><span style="color:#E6DB74;"> run</span><span style="color:#E6DB74;"> dev</span></span></code></pre></div><p>Now, it is up and running.</p><h2 id="accessing-the-dotpi-manager" tabindex="-1">Accessing the <code>dotpi-manager</code> <a class="header-anchor" href="#accessing-the-dotpi-manager" aria-label="Permalink to &quot;Accessing the `dotpi-manager`&quot;">​</a></h2><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Make sure your computer is on the same network as the one you configured for the RPi in the install step</p></div><p>Once the server is up and running, go to <a href="http://127.0.0.1:9000" target="_blank"><a href="http://127.0.0.1:9000" target="_blank" rel="noreferrer">http://127.0.0.1:9000</a></a>, and Tada! you should see your RPi in the interface:</p><p><img src="'+i+'" alt="dotpi-manager"></p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Caution! it will be loud: Do <em>not</em> plug headphones to the Raspberry Pi before the following test.</p></div><p>Now if you click on the check-sound bang (on the right-hand side of the RPI status in your manager), you should hear a full volume short white noise coming out of the mini jack output of your RPI.</p><h2 id="using-the-dotpi-manager-1" tabindex="-1">Using the <code>dotpi-manager</code> <a class="header-anchor" href="#using-the-dotpi-manager-1" aria-label="Permalink to &quot;Using the `dotpi-manager`&quot;">​</a></h2><p>The controller interface is made of three main zones:</p><ol><li>Scripting zone</li><li>List of your dotpi clients</li><li>Console</li></ol><p><img src="'+s+'" alt="dotpi-manager-controller-interface"></p><p>On the top right of the screen, you will find usefull buttons to:</p><ul><li>display in full screen mode</li><li>restart <em>dotpi</em> clients</li><li>turn off <em>dotpi</em> clients</li></ul><p><img src="'+r+'" alt="dotpi-manager-controller-interface"></p><h3 id="_1-scripting" tabindex="-1">1. Scripting <a class="header-anchor" href="#_1-scripting" aria-label="Permalink to &quot;1. Scripting&quot;">​</a></h3><p><img src="'+l+'" alt="dotpi-manager-controller-interface"></p><p>In this part of the window you can:</p><ul><li>create several tabs (&quot;pannels&quot;) corresponding to your different apps or projects. To delete a tab, press the (<code>X</code>) button at top right.</li><li>clone a project from your local directory to your Raspberry Pi using the <code>Synchronize</code> fonctionnality: Write the path of your directory and press the <code>Sync</code> button. If the <code>Watch</code> mode is activated, the directory on your Raspberry Pi client will be automatically updated each time the source project is modified in your local directory.</li><li>run a command using the <code>bash</code> shell: write the command and press <code>Execute</code>. To exit a process, press again the <code>Execute</code> button.</li></ul><h3 id="_2-dotpi-clients" tabindex="-1">2. <em>dotpi</em> clients <a class="header-anchor" href="#_2-dotpi-clients" aria-label="Permalink to &quot;2. _dotpi_ clients&quot;">​</a></h3><p><img src="'+c+'" alt="dotpi-manager-controller-interface"></p><p>The list of all your <em>dotpi</em> clients will appear on this part of the window. The different icons indicate if the client is (1) connected to the network, (2) connected to the Internet, (3) synchronizing directories, (4) executing a command. You may as well (5) filter actions or (6) logs, and (7) play some sound (<code>noise</code> or <code>sweep</code>).</p><h3 id="_3-logs" tabindex="-1">3. Logs <a class="header-anchor" href="#_3-logs" aria-label="Permalink to &quot;3. Logs&quot;">​</a></h3><p>The logs of your client(s) are displayed on this part of the window.</p><p>If you click on the <code>stderr</code> button (for &#39;standard error&#39;), this will filter the logs to display only the errors:</p><p><img src="'+p+'" alt="dotpi-manager-controller-interface"></p><p>Press the delete button (<code>X</code>) at top right to clear all logs.</p>',40)]))}const _=t(d,[["render",h]]);export{k as __pageData,_ as default};
